name = "distill"
resources = "gpus"

[matrix]
dataset = ["MELD--E"]
seed = ["0", "114"]

[tasks.T]
command = "uv run emotion-recognize train"
args = [
    "configs/dataset/{dataset}.toml",
    "configs/encoders/T.toml",
    "--seed {seed}",
]

[tasks.distill]
command = "uv run emotion-recognize distill"
args = [
    "configs/dataset/{dataset}.toml",
    "configs/encoders/{encoder}.toml",
    "configs/fusion/{fusion}.toml",
    "--teacher-checkpoint checkpoints/training/trainable--2/{dataset}/1xE--T/ce200638--{seed}",
    "--seed {seed}",
]
deps = ["T"]
[tasks.distill.matrix]
encoder = [
    "T+A",
    "T+V",
    "T+A+V",
]
fusion = [
    "vallina",
    "self-attention",
    "cross-attention",
]
